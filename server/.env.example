# Keep this file secret-free. Put the real key in server/.env (which must stay untracked).
NAYAN_REASONING_BACKEND=openai
OPENAI_API_KEY=replace_with_your_key
NAYAN_OPENAI_MODEL=gpt-5
# Optional only when using an approved compatible gateway.
# NAYAN_OPENAI_BASE_URL=https://api.openai.com/v1
