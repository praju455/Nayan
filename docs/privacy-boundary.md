# Privacy boundary

Raw screenshots, raw OCR, raw DOM/HTML, raw user tasks, passwords, token-vault mappings, and unredacted visual crops are local-only artifacts. A payload is assembled exclusively from the new sanitized artifact. If scanning or validation fails, Nayan blocks the request; it never falls back to raw transmission.
