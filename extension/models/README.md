# Local models

This directory holds signed, quantized ONNX bundles supplied during model selection. It intentionally contains no placeholder model: Nayan runs in DOM/ARIA semantic mode until a verified local model is installed, and never uploads a raw frame as fallback.
